services:
  - type: web
    name: auto-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn ui:server --workers 1 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
